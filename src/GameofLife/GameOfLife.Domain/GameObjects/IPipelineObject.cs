namespace GameOfLife.Domain.GameObjects
{
    public interface IPipelineObject
    {
        //Empty RabbitMq, external serialization was here in order to serialize / deserialize off a queue.
    }
}
